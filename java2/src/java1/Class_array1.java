package java1;

import java.util.Array;

//public import
public class Class_array1 {

	// 전역 배열변수 작성

	// 외부 파일에 있는 class + array(배열)
	public void userlist() {
		
		//	String user[]= {"hong", "kim", "park"};
		Data1.java
		System.out.println(user.toString());
		System.out.println(Array.toString(user));
	}

	/*
	 * package java1;
	 * import java.util.Array; //class_array3~
	 * //public import public class Class_array1 { data1 db = new Data1(); //외부파일
	 * class를 로드 system.out.prinln(Array.toString(db.user)); //외부 전역변수 값
	 * db.userlight(); //외부파일 메소드 호출
	 */

}
